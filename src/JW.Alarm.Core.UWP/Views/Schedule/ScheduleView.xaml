<Page
    x:Class="JW.Alarm.Core.UWP.ScheduleView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:JW.Alarm.Core.UWP"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Grid Grid.Row="0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="5*"/>
                <ColumnDefinition Width="5*"/>
            </Grid.ColumnDefinitions>
            <Button Grid.Column="0" Content="Cancel"  HorizontalContentAlignment="Center" HorizontalAlignment="Stretch"></Button>
            <Button Grid.Column="1" Content="Save" HorizontalContentAlignment="Center" HorizontalAlignment="Stretch"></Button>
        </Grid>
        <Grid Grid.Row="1" VerticalAlignment="Top">
            <StackPanel Orientation="Vertical">
                <TextBlock TextAlignment="Center" HorizontalAlignment="Stretch" Text="Alarm Time"></TextBlock>
                <TimePicker HorizontalAlignment="Stretch" />
            </StackPanel>
          
        </Grid>
    </Grid>
</Page>
